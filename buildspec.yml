version: 0.2
run-as:root
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm i -g yarn
  build:
    commands:
      - yarn install
      - yarn build

artifacts:
  files:
    - '**/*'