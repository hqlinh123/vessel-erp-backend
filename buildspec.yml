version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm i -g yarn
  build:
    commands:
      - yarn install
      - yarn build

artifacts:
  files:
    - '**/*'